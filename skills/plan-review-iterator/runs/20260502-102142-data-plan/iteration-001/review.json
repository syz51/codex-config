{
  "summary": "Formal read-only plan review completed. I inspected /Users/roy/Downloads/data-plan.md plus local CLI, preprocessing, docs, and test context. Main result: the direction is reasonable, but the plan has several blockers around bypass consistency, date/data-availability semantics, live-fetch defaults, and stale-label migration.",
  "markdown_response": "**Findings**\n\n1. **High: Execution-ready build remains a bypass path**\n   The plan says `quant-build-production-dataset` becomes the single canonical refresh entrypoint, but explicitly keeps `quant-build-execution-ready-dataset` active without a bypass flag. That command is a build command, not just validation, and its current defaults still point at `a_share_all_v1_full_20260424` and `20080101_20260424_full`. This leaves a direct stale artifact path outside the unified refresh.\n   Recommendation: either deprecate `quant-build-execution-ready-dataset` behind `--allow-manual-data-step`, or make it an internal/diagnostic command requiring explicit input paths and output label. Keep only `quant-gate-execution-ready-dataset` freely callable.\n\n2. **High: Default live refresh is a major behavior change and needs cache/rate-limit semantics**\n   Current project guidance treats full live refresh as intentional, and raw-store refresh only when a gap is proven. The plan flips default `quant-build-production-dataset` into a live Tushare/BaoStock/GDELT refresh across all non-Level-2 families. That can consume quotas, fail on network/vendor availability, and make normal preprocessing nondeterministic. Current macro GDELT is opt-in via `--enable-gdelt-doc-api`; the plan makes `--skip-gdelt-doc-api` the opt-out.\n   Recommendation: define default as cache-first plus freshness checks, with explicit `--refresh-*` modes for live fetches. Add `--prefer-cache`/resume checkpoints for BaoStock, rate-limit controls, and step-level skip reasons for already-current artifacts.\n\n3. **High: Latest-date resolver is underspecified for partial trading days and monthly ML eligibility**\n   “Latest completed SSE open day <= today” is not enough. On an SSE trading day before post-close vendor data is complete, resolving to today can fetch partial or empty data. Monthly ML eligibility also depends on the month-end signal date and the next rebalance-day data. Existing monthly schedule logic requires a next open day after month-end; daily backfill only covers open days through its own `end_date`.\n   Recommendation: make the resolver return separate `requested_end_date`, `daily_end_date`, `monthly_signal_date`, `monthly_rebalance_date`, and `ml_eligible`. Define Asia/Shanghai cutoff time, vendor-lag policy, and exact behavior when next rebalance data is unavailable. Test trading day before close, after close, weekend/holiday, month-end, and post-month-end rebalance availability.\n\n4. **High: Label generation risks breaking downstream defaults**\n   The plan says labels are generated from `START/END`, but the repo has many downstream defaults and constants tied to `a_share_all_v1_full_20260424` and related `20080101_20260424` paths, including production, execution-ready, factor registry, tournament, feature views, docs, and tests. A new label scheme will leave commands reading stale artifacts unless the migration is explicit.\n   Recommendation: centralize label/path derivation and migrate downstream defaults deliberately. Preserve the existing semantic prefix, e.g. `a_share_all_v1_full_{effective_end}`, unless there is a reason to include start date. Add tests that the dry-run plan and generated defaults do not mix new labels with stale `20260424` orchestration paths.\n\n5. **Medium: Deprecated CLI classification is incomplete**\n   The deprecation list covers many refresh/build CLIs, but the repo also exposes related scripts such as `quant-import-benchmark-constituents`, `quant-build-official-industry-manifest`, `quant-confirm-stock-basic-patches`, and already-deprecated `quant-build-event-layer`. These may or may not need deprecation, but the plan does not classify them, so bypass routes can remain by omission.\n   Recommendation: create an explicit classification table for every `[project.scripts]` entry: canonical, gate/validation, audit-only, manual-data-step, historical-deprecated, research-bypass, or unaffected. Add a test that compares `pyproject.toml` script names to that classification.\n\n6. **Medium: The shared orchestration layer needs a concrete contract**\n   Current `cli_build_production_dataset.py` shells out with `subprocess.run`, while the individual CLIs are mostly argparse wrappers around live IO. The plan says to add shared step functions but does not define their interface, idempotency, dry-run behavior, or how direct CLI bypass guards avoid blocking internal orchestration.\n   Recommendation: define a small step contract: inputs, outputs, dependencies, `dry_run`, `status`, `skip_reason`, `artifact_paths`, timings, and exception policy. Add tests proving production orchestration does not call public console scripts/subprocess, while direct deprecated CLIs fail before token loading unless `--allow-manual-data-step` is present.\n\n7. **Medium: Dependency order is too coarse for implementation**\n   Several planned steps depend on artifacts not named in the sequence. For example, daily store currently needs monthly-universe/history inputs for some modes; BaoStock sentinels also default to monthly universe and benchmark constituents; ML-ready consumes benchmark returns, ETF proxy returns, observation quality, events, PIT industry, and fundamental features. The plan lists families, but not the actual artifact DAG.\n   Recommendation: turn the step list into a DAG with concrete paths and labels per step. Dry-run should expose that DAG, not just a flat command list.\n\n8. **Medium: Public interface does not handle existing flag migration**\n   Existing documented usage includes `--refresh-monthly`, `--refresh-raw-store`, `--start-date`, `--output-label`, `--scope-name`, and `--skip-fundamentals`. The new public interface lists only `--end-date`, `--dry-run`, `--strict-full`, `--skip-baostock`, and `--skip-gdelt-doc-api`. Silent removal would break current docs and scripts.\n   Recommendation: explicitly state which flags remain, which become aliases, and which are removed. Prefer keeping backwards-compatible aliases with deprecation warnings for at least one pass.\n\n9. **Low: Test plan should cover stale-path and no-subprocess regressions more directly**\n   The test plan has good categories, but it should include checks that generated execution-ready, benchmark, ETF, regime, macro, noise, and ML paths are all derived from the same resolved context. It should also include a regression test that production orchestration cannot invoke deprecated public CLIs.\n   Recommendation: add tests over the dry-run JSON schema and step DAG, plus a monkeypatched no-subprocess test.\n\n**Overall**\n\nThe plan is directionally useful, but I would not approve it as execution-ready yet. The largest correction is to separate “canonical orchestration” from “always live refresh,” then lock down resolver semantics and bypass classification before implementation. No external research was necessary; local repo context was sufficient for this review.",
  "findings": [
    {
      "id": "F-001",
      "severity": "high",
      "confidence": 0.94,
      "title": "Execution-ready builder remains an undeclared bypass path",
      "location": "/Users/roy/Downloads/data-plan.md:33",
      "evidence": "The plan keeps `quant-build-execution-ready-dataset` active and not deprecated at lines 33-38, but the command is a build path. Local code in `src/quantresearch/cli/cli_build_execution_ready_dataset.py` defaults to stale `a_share_all_v1_full_20260424` and `20080101_20260424_full` inputs.",
      "recommendation": "Deprecate `quant-build-execution-ready-dataset` behind `--allow-manual-data-step`, or require explicit paths/output label and document it as diagnostic/internal. Leave only the execution-ready gate freely callable.",
      "actionable": true
    },
    {
      "id": "F-002",
      "severity": "high",
      "confidence": 0.9,
      "title": "Default live refresh risks quota, network, and nondeterministic failures",
      "location": "/Users/roy/Downloads/data-plan.md:4",
      "evidence": "The plan makes the default command refresh daily/reference data and all non-Level-2 families. Current repo guidance says full live refresh is intentional-only and raw-store refresh is only for proven gaps; current GDELT DOC API is opt-in via `--enable-gdelt-doc-api`, while the plan makes it opt-out with `--skip-gdelt-doc-api`.",
      "recommendation": "Make the default cache-first with freshness checks. Use explicit refresh flags for live network fetches, add rate-limit/checkpoint/resume semantics, and keep BaoStock/GDELT failure isolated as diagnostics unless strict mode requests otherwise.",
      "actionable": true
    },
    {
      "id": "F-003",
      "severity": "high",
      "confidence": 0.88,
      "title": "Date resolver is underspecified for completed data and ML eligibility",
      "location": "/Users/roy/Downloads/data-plan.md:8",
      "evidence": "The plan says omitted `--end-date` resolves to latest completed SSE open day <= today, and ML-ready only builds when monthly signal plus next rebalance-day data exists. Existing monthly schedule code requires a next open day after month-end, while daily backfill only fetches open days through its own end date.",
      "recommendation": "Define resolver outputs for `requested_end_date`, `daily_end_date`, `monthly_signal_date`, `monthly_rebalance_date`, and `ml_eligible`, including Asia/Shanghai cutoff time and vendor-lag policy. Add tests for trading-day intraday, weekend/holiday, month-end, and post-month-end rebalance availability.",
      "actionable": true
    },
    {
      "id": "F-004",
      "severity": "high",
      "confidence": 0.86,
      "title": "Generated START/END labels could leave downstream commands on stale artifacts",
      "location": "/Users/roy/Downloads/data-plan.md:9",
      "evidence": "The plan requires generated labels from START/END and no hardcoded 20260424 orchestration paths. Local search shows many downstream defaults still hardcode `a_share_all_v1_full_20260424` and `20080101_20260424`, including production constants, execution-ready constants, factor/tournament CLIs, docs, and tests.",
      "recommendation": "Centralize label/path derivation and explicitly migrate downstream defaults. Prefer preserving the semantic label shape `a_share_all_v1_full_{effective_end}` unless START is truly needed. Test that a dry-run for a new end date does not mix new and stale labels.",
      "actionable": true
    },
    {
      "id": "F-005",
      "severity": "medium",
      "confidence": 0.82,
      "title": "Deprecation list does not classify all related public scripts",
      "location": "/Users/roy/Downloads/data-plan.md:25",
      "evidence": "The plan lists many direct data-refresh/build commands, but `pyproject.toml` also exposes related commands such as `quant-import-benchmark-constituents`, `quant-build-official-industry-manifest`, `quant-confirm-stock-basic-patches`, and already-deprecated `quant-build-event-layer`. The plan does not say whether these remain active, become manual-data-step, or are historical.",
      "recommendation": "Add a complete classification table for every `[project.scripts]` entry and a test that fails when new scripts are unclassified.",
      "actionable": true
    },
    {
      "id": "F-006",
      "severity": "medium",
      "confidence": 0.8,
      "title": "Shared orchestration contract is not defined",
      "location": "/Users/roy/Downloads/data-plan.md:22",
      "evidence": "The plan says to add shared internal orchestration so the unified command does not shell out. Current production CLI shells out via `subprocess.run`, and direct CLIs combine parsing, token loading, live IO, and artifact writes.",
      "recommendation": "Define a step interface with inputs, outputs, dependencies, dry-run behavior, status, skip reason, artifact paths, timing, and exception policy. Add tests proving production orchestration does not invoke public console scripts or subprocess and that deprecated CLIs fail before token loading without bypass.",
      "actionable": true
    },
    {
      "id": "F-007",
      "severity": "medium",
      "confidence": 0.77,
      "title": "Dependency order is too coarse to implement safely",
      "location": "/Users/roy/Downloads/data-plan.md:13",
      "evidence": "The plan lists broad families but not concrete artifacts. Local CLIs have implicit dependencies: daily store can depend on monthly universe, BaoStock sentinels default to monthly universe and benchmark constituents, ML-ready consumes observation quality, macro events, PIT industry, benchmark returns, ETF proxy returns, and fundamentals.",
      "recommendation": "Convert the sequence into a concrete DAG with path-producing and path-consuming steps. Make dry-run emit that DAG and verify each non-skipped step's declared inputs exist or are produced earlier.",
      "actionable": true
    },
    {
      "id": "F-008",
      "severity": "medium",
      "confidence": 0.78,
      "title": "Existing production flags are not migrated or deprecated",
      "location": "/Users/roy/Downloads/data-plan.md:45",
      "evidence": "The public interface omits current documented flags such as `--start-date`, `--output-label`, `--scope-name`, `--refresh-monthly`, `--refresh-raw-store`, and `--skip-fundamentals`. Current AGENTS/docs mention `--refresh-monthly` and `--refresh-raw-store`.",
      "recommendation": "State which existing flags remain, which become deprecated aliases, and which are removed. Prefer compatibility aliases with warnings for one migration cycle.",
      "actionable": true
    },
    {
      "id": "F-009",
      "severity": "low",
      "confidence": 0.76,
      "title": "Test plan misses no-subprocess and full-path-coherence checks",
      "location": "/Users/roy/Downloads/data-plan.md:57",
      "evidence": "The test plan checks dry-run, deprecation, resolver behavior, and quality gates, but not that unified orchestration avoids deprecated public CLIs or that all generated paths share one resolved label/date context.",
      "recommendation": "Add tests over dry-run JSON/DAG schema, stale-path absence across all produced artifacts, and monkeypatched no-subprocess/no-public-CLI execution.",
      "actionable": true
    }
  ],
  "unresolved_questions": [
    "Default live fetch or cache-first?",
    "Deprecate execution builder?",
    "GDELT default on?",
    "BaoStock fail hard?",
    "Label scheme exact?",
    "Old flags kept?"
  ]
}
