The previous validation command failed. Treat this as an actionable finding and patch the plan so the validation failure is addressed or clearly documented as intentionally out of scope.

Validation log:

```text
{{VALIDATION_CONTEXT}}
```
